/** @type {import("prettier").Config} */
const config = {
    plugins: ['prettier-plugin-astro'],
    singleQuote: true,
    trailingComma: 'all',
    printWidth: 80,
    tabWidth: 4,
};
export default config;
